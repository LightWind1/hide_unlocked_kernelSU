# Intentionally left without property assignments.
#
# This module only changes properties that already exist and have a mismatched
# value. Conditional checks live in post-fs-data.sh and service.sh; using static
# system.prop entries would create missing properties during early boot.
